1. Добавил зависимость hibernate core (зависимость для драйвера jdbc H2 уже была с прошлого урока)

1. Создать базу данных Student с полями id, name, mark (видимо имелось в виду создать таблицу с полями)
    скрипт создания таблицы находится в файле init.sql

2. Создать сущность Student и добавить в нее аннотации. Поле id должно заполняться автоматически
    создал класс Student, добавил аннотации,
    для автоматического заполнения поля id написал аннотацию @GeneratedValue(strategy = GenerationType.IDENTITY)

2. Создать конфигурационный файл hibernate.cfg.xml, в котором указать свойства для подключения к БД и список классов с аннотациями Entity
    создал в папке resources файл, указал в нем класс Student

4. Создать класс со статическим методом, который возвращает объект SessionFactory.
    создал (скопировал из интернета) класс HibernateUtil со статическим блоком инициализации и методом getSessionFactory

5. Создать DAO-слой с операциями добавления, удаления, изменения сущности, выборки записи по ID и всех записей.
    сделал файл StudentDaoImpl.java (в папке dao)

6. Добавить 1000 записей в таблицу Student.
    в классе Main создал метод создания коллекции любого заданного количества студентов createListStudents, на примере ограничился 10

 7. Проверить работоспособность приложения, выполнить методы по удалению, изменению, добавлению записей, а также выборки одной и всех записей.
    удвление +, добавление записей +, выборка 1 записи +, выборка всех записей +, изменение 1 записи +, все проверил, работает даже!